FROM node:20-bookworm-slim

WORKDIR /app

COPY server/package.json server/package-lock.json ./
# 生产入口与 pm2 一样走 tsx；tsx 在 server 的 devDependencies 里
RUN npm ci

COPY server/tsconfig.json ./
COPY server/src ./src

ENV NODE_ENV=production
EXPOSE 3001

CMD ["npx", "tsx", "src/server.ts"]
